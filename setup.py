import setuptools


setuptools.setup(
    name='TypeBarrier',
    version='0.0.1',
    author='Tim Simpson',
    packages=setuptools.find_packages(exclude=['tests']),
    include_package_data=True,
    install_requires=[],
    classifiers=[],
)
